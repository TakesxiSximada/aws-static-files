version: 2

jobs:
  build:
    docker:
      - image: debian:stretch

    steps:
      - checkout
      - run:
          name: Install dependencies
          command: yarn
      - run:
          name: Build statif files
          command: yarn build
