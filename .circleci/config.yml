version: 2

jobs:
  build:
    docker:
      - image: node:9.2-alpine

    steps:
      - checkout
      - run:
          name: Install yarn
          command: npm install -g yarn
      - run:
          name: Install dependencies
          command: yarn
      - run:
          name: Build statif files
          command: yarn build
